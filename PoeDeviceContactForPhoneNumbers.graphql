query PoeDeviceContactForPhoneNumbers($phoneNumbers: [String!]!) {
  poeDeviceContactForPhoneNumbers(phoneNumbers: $phoneNumbers) {
    poeUser {
        id
        uid
        ...UserSnippetFragment
    }
    contactPhoneNumber
  }
}
