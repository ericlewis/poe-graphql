mutation AddPostViewMutation($postId: BigInt!) {
    addPostView(postId: $postId) {
        post {
            id
            __typename
            viewCount
            viewerHasViewed
        }
        status
    }
}
