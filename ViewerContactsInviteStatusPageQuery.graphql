query ViewerContactsInviteStatusPageQuery($phoneNumbers: [String!]!) {
    viewer {
        id
        primaryPhoneNumberCountryCode
        inviteStatusForContacts(phoneNumbers: $phoneNumbers)
    }
}
