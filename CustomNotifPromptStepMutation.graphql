mutation CustomNotifPromptStepMutation(
    $state: PushNotifAcceptanceState!
    $source: PushNotifPromptSource!
) {
    customNotifPromptStep(
        state: $state,
        source: $source,
    ) {
        viewer {
            id
            uid
        }
    }
}
